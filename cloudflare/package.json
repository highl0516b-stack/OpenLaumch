{
  "name": "openlaunch-gateway",
  "version": "1.0.0",
  "description": "OpenLaunch API Gateway - CloudFlare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "publish": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "format": "prettier --write src tests"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241201.0",
    "@types/node": "^20.17.0",
    "esbuild": "^0.21.0",
    "happy-dom": "^15.11.7",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5",
    "wrangler": "^3.99.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
}
