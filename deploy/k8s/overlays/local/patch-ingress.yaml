apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: openlaunch-web
spec:
  rules:
    - host: openlaunch.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: openlaunch-web
                port:
                  number: 80