apiVersion: v1
kind: Service
metadata:
  name: openlaunch-api
  namespace: openlaunch
  labels:
    app.kubernetes.io/name: openlaunch-api
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: openlaunch-api
  ports:
    - name: http
      port: 80
      targetPort: http
